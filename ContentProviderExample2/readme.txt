ContentProviderExample2

  -	Uses the ContentProvider for the following operations:
  	  - Creates SQLite database
  	  - Uses SimpleCursorAdapter for listing the records in the table
  	  - Overrides both optionsMenu and contextMenu
  	  - Can add / delete records in the table using menus
  	  - ContentProvider should be registered in the AndroidManifest.xml
  